version: '3'

services:
  restauDB:
    image: postgres:16.4
    container_name: citezen
    restart: always
    ports:
      - 5432:5432 
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_DB=${DB_NAME}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    volumes:
      - ./postgres:/var/lib/postgresql/data
